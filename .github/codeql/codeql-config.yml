paths:
  - editor.md
  - fontawesome
query-filters:
  - exclude:
      problem.severity:
        - warning
        - recommendation
  - exclude:
      id: js/useless-regexp-character-escape
  - exclude:
      id: js/inefficient-regexp
